package com.kevin.nestedloops;

import java.util.List;
import java.util.Stack;

/**
 * Created by kevinlanaghan on 2/2/17.
 */
public class Recursion
{
/*
    public int traverse(String[][] lists, int index, int level, String[] results)
    {

        int nextLevel = 0;

        if (index < lists.length -  1)
        {
            results[index] = lists[index][level];
            traverse(lists, ++index, level, results);

        }
        else
        {
            level = level + 1;
            for (int i = 0; i < lists[index].length)
            {

            }
        }

        return level++;
    }


    private void printResults(Stack<String> inResults)
    {

    }

    public static void main(String inArgs[])
    {
        String[][] combinations = new String[3][4];
    }
    */
}